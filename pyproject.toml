[project]
name = "rage-quit-predictor"
version = "0.1.0"
description = "Custom transformer to predict rage quits from in-game behavioral sequences (Dota 2)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
